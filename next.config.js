/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['img.freepik.com'], // Add the hostname(s) you want to allow
    },
  };
  
  module.exports = nextConfig;
  